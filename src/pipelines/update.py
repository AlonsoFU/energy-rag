# src/pipelines/update.py — Phase 11 will implement the real diff + descarga.
def run_update(dry_run: bool = False) -> dict:
    print(f"[update] dry_run={dry_run} (not yet implemented; see Phase 11)")
    return {"status": "stub"}
