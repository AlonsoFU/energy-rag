# Handoff — Energy-RAG (2026-06-02)

> Continúa `handoff-2026-06-01.md`. Detalle: `docs/campaign-2026-06-02.md`. Pipeline: `architecture-status.md` §8/§8b.

## TL;DR
Sesión de **auditoría de golds + 1 experimento de arquitectura**. El "frente" del 06-01
(distractor/multiparte) era **artefacto de medición**: corregir 8 golds recuperó **+8 cita_ok sin
tocar el sistema**. El lever probado (term-prefix de glosario) se **RECHAZÓ** (held-out no mejora).
Insight nuevo: el cuello de las definiciones es **generación/elección de cita**, no recall.

## NÚMEROS HONESTOS ACTUALES (config GPU BGE fp16 k10, golds corregidos)
| set | cita_ok | grounding |
|---|---|---|
| dev (44) | **37/44** | 43/44 |
| holdout (18) | **16/18** | 18/18 |
| extremo (18) | **17/18** | 18/18 + 4/4 rechazos |

(Mejora vs 06-01: +5 dev, +3 extremo — **todo por gold corregido, cero cambio de arquitectura**.)

## QUÉ CAMBIÓ (en la rama, para PR)
- **8 golds corregidos** (verificados leyendo la ley) en `data/eval/queries_extreme.jsonl` y
  `queries_independent.jsonl`. Clusters: VATT/AVI/VI todos a `1146553/48`; energía firme mal atribuida
  al reglamento (es LGSE 225); obra→remuneración; plan transmisión vs energético. Ver tabla en campaign-2026-06-02.
- **Scripts nuevos** (referencia): `scripts/diag_authority.py`, `scripts/exp_glossary_prefix.py`
  (apply/revert, A/B-safe), `scripts/diag_glossary_rank.py`.
- **Índice SIN cambios** (el term-prefix se aplicó y se REVIRTIÓ; backup en `data/eval/results/glossary_prefix_backup.json`).

## LEVER RECHAZADO HOY (no repetir)
**Term-prefix determinista de glosario**: anteponer lista de términos a `contextual_text` de chunks-glosario.
A/B: dev +1, holdout 0, extremo −1. Causa: (a) boostear *todos* los glosarios → compiten entre sí
(un glosario rival le robó el lugar al 225); (b) las ganancias de retrieval (gold en pool@1-2) NO se
convirtieron en cita_ok. Net≈0, held-out no mejora → DESCARTADO.

## SET NUEVO `queries_complex_v2.jsonl` (54q complejas, gold leído de la ley)
Creado para validar generalización en casos NO vistos y más difíciles (situacional, multiparte,
condicional, procedimental, paráfrasis, cross-norma, negación). Gold verificado contra el texto
(evidencia verbatim en cada línea; 0 golds inexistentes). Autoría por 5 agentes temáticos.
- **Resultado (BGE CPU, k10): cita_ok 52/54 = 96%, grounding 54/54, answered 54/54.** Situacional/
  condicional/multiparte/procedimental/cross-norma = 100%; paráfrasis 8/9; negación 1/2.
- **Confirma**: el sistema maneja compositivas/situacionales mucho mejor de lo que los golds buggy
  decían. El "frente" del 06-01 era medición.
- **CAVEAT**: queries autoradas leyendo el artículo → posible sesgo de recuperabilidad (vocabulario
  alineado con el gold). 96% es techo bajo autoría gold-anclada, no garantía sobre fraseo a ciegas.
- **Debilidad real consistente = NEGACIÓN/COMPLEMENTO**: "lo que la ley NO sujeta a X" → cita el
  artículo positivo (147) en vez de su negación (149). Mismo patrón en holdout. Clase de lever futuro.
- **GOTCHA medido**: BGE en GPU + 9b residente OOMea con queries LARGAS (techo 8GB). Para sets de
  queries largas usar `BGE_DEVICE=cpu` (mismo ranking, más lento). Result: `CX_v2.json`.

## SET v3 ADVERSARIAL `queries_complex_v3.jsonl` (51q) — RELOCALIZA EL FRENTE
Segundo set, fraseo a ciegas para reducir sesgo de recuperabilidad, cargado a clases difíciles
(negación 10, coloquial 11, adyacente 7, crossnorma 5, multihop 5, temporal 5, cuantitativo 5,
distractor 3). Gold verificado de la ley (51/51 existen). **cita_ok 43/51 = 84%** (BGE CPU k10).
- **Fuerte (~100%)**: negación **10/10**, adyacente 7/7, distractor 3/3, crossnorma 5/5, cuantitativo
  5/5, temporal 5/5. ⇒ Maneja bien lo COMPLEJO con vocabulario legal. (Mi hipótesis de que la
  negación era el cuello quedó REFUTADA.)
- **DÉBIL = COLOQUIAL: 5/11 (45%), answered solo 6/11.** 5 queries en lenguaje cotidiano fueron
  RECHAZADAS por el guard de off-topic (es LÉXICO: sin solape de vocabulario con el corpus → trata
  in-domain como fuera de dominio). Ej: "máquina para respirar que necesita electricidad"→207-2,
  "el cajón de la pared y el aparatito que cuenta"→empalme/medidor 139 bis, "fábrica que gasta
  mucho ¿negocio el precio?"→149. Todas RECHAZADAS.
- **multihop 3/5**: 1 no hizo el 2º salto (reglamento→def LGSE 225, se quedó en el reglamento).
- **Artefacto**: 2 misses citan variante de sub-artículo ("29819/2 D" vs "2°", "1 D" vs "1°") — revisar
  si es numeración duplicada en datos o gold.
- **CONFIRMA el sesgo de recuperabilidad**: v2 (vocab del artículo) 96% vs v3 coloquial 45%. El frente
  real de GENERALIZACIÓN = el guard de off-topic LÉXICO sobre-rechaza queries cotidianas in-domain +
  falta mapeo lenguaje-cotidiano→término (reproduce hallazgo histórico "el sistema exige nombrar el término").

## EL FRENTE ABIERTO (próxima sesión)
0. **NUEVO #1 — Off-topic guard semántico + expansión de query** (lo que v3 destapó): el guard léxico
   rechaza queries coloquiales in-domain (5/11). Hacerlo semántico (embeddings) y/o expandir la query
   (HyDE ya existe, flag OFF) para puentear lenguaje cotidiano→término. Medir contra v3 coloquial.
1. **Lever de GENERACIÓN / disciplina de citación** (el cuello real): en queries definicionales el LLM
   cita el artículo vecino aunque el gold esté en pool@1-2. Probar: preferir/anclar el artículo que
   define "X:" cuando la query es "qué es X" (conecta con `_anchor_authoritative_citation`, flag OFF
   hoy — medirlo). SIN re-embed, cheap-ish.
2. **Misses reales que quedan** (no gold): servicios complementarios, cogeneración, tasa de
   actualización (def menor en 225, pierde vs art monotemático 182 bis), max energía almacenamiento
   (gold 225 dudoso: el término "Máxima Energía de Almacenamiento" está en 250604/13 ck29 — verificar),
   panel financiamiento 212 vs 212-13, expansión 91 vs 92 (adyacente).
3. **Contextual chunks full** (gap #2, resumen LLM por chunk, ~3900, overnight) sigue PENDIENTE — pero
   ojo: el insight de hoy sugiere que el recall no es el cuello para definiciones.

## GOTCHAS (vigentes, ver también handoff-2026-06-01 §gotchas)
- "Arreglar el medidor primero": antes de cualquier lever, auditar golds de los misses (leyendo la ley).
  El held-out (06-01) está limpio; dev y extremo (autoría rápida) tenían los errores.
- term-prefix `exp_glossary_prefix.py revert` restaura embeddings desde backup (re-embebe el texto original).
- 2 archivos ajenos sin commitear: `queries_diverse.jsonl` (M), `_rem.jsonl` (??) — NO commitear, revisar.
- Resto de infra/GPU: idéntico a handoff-2026-06-01 (venv-gpu cu118, BGE fp16, Postgres 5434, HF_HUB_OFFLINE=1).
