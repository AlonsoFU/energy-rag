# Handoff — Energy-RAG (2026-05-20, consolidado)

> Documento canónico para retomar tras /compact. Autosuficiente: estado +
> commits + hallazgos + números + plan. Reemplaza handoff-2026-05-16 como
> snapshot vigente. Branch `feat/postgres-rag-v1`.
>
> Decisiones de arquitectura: `docs/decisiones-arquitectura.md` (actualizado
> esta sesión).

---

## TL;DR

Sesión larga de **medición honesta + cuello de botella real**. El "+3pp
grounding" del handoff anterior **NO SOBREVIVIÓ ESCRUTINIO**: artefacto de
instrumento. El cuello real era un cuelgue determinista de Ollama (qwen3.5)
con JSON-schema constrained-decoding — diagnosticado y arreglado.

**Estado final**: sistema funcional sin cuelgues, grounding por categoría
estable y reproducible (varianza ≈ 0 entre corridas idénticas).

---

## Lo que se refutó del handoff anterior

El "+3pp eléctrico" se sostenía en 3 defectos apilados:

1. Set `queries_electrico.jsonl` (31q) seguía **35% contaminado** — el split
   previo quitó concursal (Ley 20720) pero dejó pasar:
   - `1207690` Decreto 262 telepeaje vial (×9)
   - `1007469` DFL 1 Ley de Tránsito (×1)
   - `1199483` Ley 21647 reajuste sueldos sector público (×1)
2. `grounding% = passes / n_with_generation` **excluía los cuelgues** del
   denominador → inflaba.
3. `grounding_pass` bajo constrained-decoding ≈ tautológico: el sampler
   restringe citas al pool, el verifier las compara contra ese mismo pool.
   Dio PASS a query de aguinaldos en eval eléctrico. Mide guardrail, NO
   correctitud.

**Lección reforzada**: auditar el eval clasificando CADA norma contra la DB,
no por keyword/síntoma del run.

---

## Causa raíz del cuelgue de Ollama (resuelta)

Ollama `qwen3.5:9b` deadlockea cuando recibe JSON-schema-constrained
decoding combinado con prompts grandes y/o el flag `think=false` (que SÍ
usábamos). Confirmado por:

- Probe sistemático: TODOS los JSON schemas cuelgan, sin importar contenido
  ni tamaño del enum (incluso 2 entradas con dígitos limpios).
- Sin schema, generación plana funciona perfecto.
- Issues upstream que documentan exactamente nuestro caso:
  [#15540](https://github.com/ollama/ollama/issues/15540) (qwen3.5 no enforced),
  [#15260](https://github.com/ollama/ollama/issues/15260) (think=false rompe format),
  [#15502](https://github.com/ollama/ollama/issues/15502) (repetition loop),
  [#8063](https://github.com/ollama/ollama/issues/8063) (general).

**Fix aplicado**: **patrón híbrido** — generación libre + `verify_citations`
post-hoc + retry con prompt estricto. Industria-recomendado cuando el
constrained-decoding del runtime no es confiable.

---

## Los 6 fixes aplicados esta sesión (todos legal-safe)

| # | qué | dónde | por qué |
|---|---|---|---|
| 1 | `ollama_num_ctx: 16384` (8K→16K) | `src/core/config.py` + `src/components/llm.py` | prompt 15k tokens desbordaba ventana 8k → 1ra clase de cuelgue (Acometida 900s→47s) |
| 2 | `prompt_doc_char_budget: 45000` | `config.py` + `src/pipelines/prompts.py` + `generate.py` | recorta tail de docs si pasan el budget → garantía determinista contra overflow |
| 3 | `use_constrained_decoding: False` (patrón híbrido) | `config.py` + `generate.py` | desactiva JSON-schema (Ollama bug), pasa la garantía al verifier post-hoc |
| 4 | `strip_malformed_citations` | `src/pipelines/grounding.py` + `generate.py` | borra `[Art. ag de ...]` (citas inventadas con identificador no-numérico) del texto entregado al usuario |
| 5 | `verify_citations_against_corpus` | `grounding.py` + `generate.py` | fallback del verifier: si la cita NO está en el top-10 retrieved pero SÍ existe en la DB completa → válida (legal-safe, strict-exact) |
| 6 | system prompt limpiado | `prompts.py` | sacó instrucciones de formato JSON (`citations: []`) que en modo híbrido producían cola residual |

---

## RESULTADOS — eval `queries_balanced.jsonl` (80q, 3 categorías)

| categoría | n | answered | rechazo explícito | empty (skip LLM) | grounding_pass | recall@5 norma |
|---|---|---|---|---|---|---|
| **in_domain** (energía) | 50 | **74%** | 2% | 24% | 74% | 84% |
| **off_domain_corpus** (peaje/tránsito/sueldos) | 15 | **87%** | 0% | 13% | 87% | 87% |
| **off_corpus** (cocina, medicina, palabras inventadas) | 15 | 0% | **100%** ✅ | 0% | 100% | n/a |

**Latencias** (todas las corridas, varianza casi nula):
- p50 ≈ 26s · p95 ≈ 42s · p99 ≈ 47s
- **Cuelgues ≥5min: 0** (era 3 antes del fix)

**Varianza run-a-run**: baseline vs replica → **idénticas en todas las
métricas**. Antes con cuelgues había ±3-5pp; el patrón híbrido eliminó la
fuente principal de no-determinismo.

---

## El techo real ahora: recall+art = 76% en in_domain

12/50 queries in_domain tienen el artículo correcto **fuera del top-5**
retrieved. El eval runner (`deepeval_runner.py:174`) skip-ea la llamada al
LLM cuando `full_hit=False` ("para ahorrar tiempo"). Por eso `gen_ms=0` y
`answer=""` en esas 12 — **NO es un rechazo del LLM**, es un shortcut del
runner.

**Implicación**: si quisiéramos ver el comportamiento real del sistema en
producción (que SÍ generaría sobre esas 12), habría que cambiar el runner.
Es trivial y útil — pendiente.

---

## Experimentos que NO movieron la aguja

| qué | resultado | conclusión |
|---|---|---|
| `RETRIEVAL_POOL_DEPTH=100` (pool BM25+vector de 50→100) | -2pp answered, -4pp recall norma | el pool más grande mete ruido en RRF; **NO subir** |
| `--top-k 15` (15 docs al LLM en vez de 10) | métricas idénticas | el shortcut del runner mira top-5; subir top-k no cambia el skip; **NO subir** |
| Variance check (mismo HEAD, mismo set) | idénticas a baseline | confirma reproducibilidad |

---

## Commits de esta sesión (branch feat/postgres-rag-v1)

| Commit | Qué |
|---|---|
| _(pendiente)_ | fix(ollama-hang): num_ctx, budget, hybrid, corpus-verify, strip, prompt-clean |
| _(pendiente)_ | test(eval): herramientas de eval (analyze, build_balanced, probes) + sets canónicos |
| _(pendiente)_ | docs: handoff 2026-05-20 + ADR actualizado |

---

## Cómo retomar en 3 min (post-compact)

```bash
cd /home/alonso/Documentos/Github/energy-rag-postgres-rag
git log --oneline -5
docker start energy_rag_pg 2>/dev/null; docker ps --filter name=energy_rag_pg
curl -sf http://localhost:11434/api/tags >/dev/null && echo "Ollama OK"
# Eval de referencia (set balanceado, 80q, ~30 min):
./venv/bin/python -m src eval --eval-file data/eval/queries_balanced.jsonl --top-k 10
```

Estado reconstruible 100% desde: este doc + `docs/decisiones-arquitectura.md`
+ git log + memoria del proyecto.

---

## Backlog priorizado (próximas direcciones)

Orden recomendado por ROI:

1. **Cambiar el runner para SIEMPRE generar** (trivial). Quita el `full_hit`
   shortcut → ver comportamiento real del sistema en las 12/50 que hoy no
   ven el LLM. Sin esto no sabés si el 24% empty son fallos reales o
   artefacto de medición.
2. **Re-ranker concepto-específico** (bajo costo, ataca el techo de recall+art
   directamente). Cuando query="qué es X" y X ↔ artículo definidor existe
   en la tabla `referencias`, boost masivo a ese artículo. Datos ya en DB.
3. **Mini-chunks de "summary" por concepto** (curación, sostenible). Por
   cada concepto curado, agregar un chunk corto tipo "C.O.M.A. = costo... -
   def. en Art. 49 D.10. Detalle: [...]". Sube recall en queries
   definicionales. Medio.
4. **Expandir aliases manualmente** con sinónimos comunes (bajo costo).

Para más adelante (cambios estructurales):
- HyDE / multi-query (genera reformulaciones de la query)
- Cambiar embedder (Qwen3-0.6B → BGE-M3 multilingüe)
- Probar reranker no-BGE (BGE rompió grounding -57pp en sesión previa)
- Subir a qwen3:32B o usar API paga (decisión del usuario: por ahora local)
- Vigencia/temporalidad real (Exp T-B, parsing derogaciones — sin
  implementar)

---

## Gotchas vigentes

1. **Ollama JSON-schema deadlockea** con qwen3.5 — issues upstream
   confirmados. No reactivar `use_constrained_decoding=True` salvo con
   runtime distinto (vLLM, llama.cpp directo, o API).
2. **El runner skip-ea LLM cuando `full_hit=False`** → métricas como
   "26% empty in_domain" son artefacto del runner, no del sistema en
   producción.
3. Postgres parado → `docker start energy_rag_pg`.
4. Eval set canónico: `queries_balanced.jsonl` (80q en 3 categorías).
   `queries_electrico.jsonl` viejo tiene 35% off-domain — NO usar para
   medir grounding sin filtrar.

---

## En una línea

> El "+3pp" anterior era artefacto. El cuello real (cuelgue Ollama
> determinista) está resuelto con 6 fixes legal-safe. Sistema reproducible,
> 0 cuelgues, in_domain 74% / off_domain 87% / off_corpus 100% rechazo.
> Próximo techo: recall+art = 76% en in_domain (atacar con re-ranker
> concepto-específico).

---

## ADDENDUM 2026-05-21 — set 339q + 2 fixes adicionales

Set ampliado a 339q (in_domain 279 × 3 phrasings + 30 off_domain_corpus + 30 off_corpus) para tener poder estadístico real. **El "74% answered" del set chico era artefacto del sesgo a definiciones largas** — número real más bajo y honesto. Dos cambios adicionales bajo el set grande:

### #113 — Runner SIEMPRE genera (`eval_always_generate=True`)
El runner antes skip-eaba LLM cuando `full_hit=False` para acelerar el eval. Eso inflaba el bucket "empty" con artefactos. **En producción el sistema NO tiene ese skip**.
- in_domain answered: **63% → 89%** (+26pp)
- in_domain empty: 33% → 0%
- grounding_pass: 65% → 97%
- **off_corpus refusal: 100%** (sin alucinaciones inducidas)

### #114 — Inyección curada de definiciones (`inject_curated_definitions=True`)
Patrón "qué es X" / "definición de X" / "qué significa X" → busca X exact-normalizado en `conceptos`, inyecta el artículo definidor en pos 1 del pool. Legal-safe (strict, sin fuzzy, datos curados).
- in_domain **recall+art: 67% → 71%** (+4pp, el techo de retrieval bajó)
- off_domain_corpus recall+art: 77% → 83% (+7pp)
- off_corpus refusal: 100% (sin contaminación)

### Estado final sobre 339q (allgen + inject)

| categoría | recall+art | answered | refusal |
|---|---|---|---|
| in_domain (279) | **71%** | **90%** | 10% |
| off_domain_corpus (30) | 83% | 93% | 7% |
| off_corpus (30) | n/a | 0% | **100%** ✅ |

**Próximo techo**: recall+art 71% en in_domain. Las 30% que no llegan: queries que no caen en el patrón definicional, conceptos sin entrada en la tabla `conceptos`, o aliases faltantes. Subirlo requiere curación (más conceptos/aliases) o reranker mejor.

---

## ADDENDUM 2026-05-21 — definiciones reales + vigencia + roadmap GraphRAG

### Hallazgo raíz: inject usaba MENCIONES, no DEFINICIONES
`referencias` = 4037 `cita` (menciones) + 196 implícitas + solo **55 `define_termino`** (definiciones reales). El inject usaba TODAS → elegía una mención arbitraria. Por eso el 76/82 de los "fallos" in_domain eran **norma-mismatch** (término definido/mencionado en varias normas, inject elegía una, eval esperaba otra). El eval también se generaba de menciones → expected arbitrario.

### Solución (hecha, medida)
1. **`scripts/build_definitions_auto.py`**: crea conceptos + aristas `define_termino` desde glosarios (`TÉRMINO: definición`). Determinista, legal-safe. **Conceptos 286→334, define_termino 55→222, conceptos con definición real 30→180.**
2. **inject usa solo `define_termino` + vigencia por fecha** (`concept_injection._concept_index`): cuando un término está en varias normas, gana la más reciente. (V.A.T.T. → Decreto 37 2021, no Decreto 10 2019.)
3. **Eval regenerado desde `define_termino`** (`queries_balanced_v3.jsonl`, 333q): expected bien definido.

### Resultado (queries_balanced_v3, 333q)
| categoría | answered | refused | grounding |
|---|---|---|---|
| in_domain (273) | **99%** | 1% | 96% |
| off_domain_corpus (30) | 97% | 3% | 100% |
| off_corpus (30) | 0% | **100%** ✅ | 100% |

recall+art 100% es por construcción (inject pone el artículo). Lo real: answered 99% (vs 63% baseline) + off_corpus refusal 100% intacto (sin alucinación inducida). Spot-check de 12 respuestas: 12/12 correctas de contenido.

### Roadmap GraphRAG (nuevo)
`docs/superpowers/plans/2026-05-21-graphrag-roadmap.md` — 8 fases hacia GraphRAG híbrido. Spec de extracción en `docs/superpowers/specs/2026-05-21-extraction-pipeline-design.md`.

**Evaluación honesta del roadmap (importante):**
- Fases 0-1 (definiciones, aliases) = **resultado seguro y ya medido**.
- Fases 3-6 (relacional, traversal, global) = **APUESTA condicionada** a que las preguntas reales sean multi-hop — algo que NO medimos. El estándar dice GraphRAG gana en multi-hop (86% vs 32%) PERO solo si tenés multi-hop. Si las preguntas son definicionales (como todo el eval actual), no aporta.
- Fase 2 (norma→norma) rinde poco: las derogaciones apuntan a leyes fuera del corpus.
- **NO construir Fases 3-6 sobre fe.** Primero medir (Fase 7-mini): ¿las preguntas reales son relacionales? Falta: ejemplos reales de preguntas de usuarios.

### Regla de resolución de entidades (decidida por usuario)
Término en varias normas → 1) excluir derogadas/obsoletas, 2) preferir mismo ámbito, 3) desempatar por fecha, 4) si ambiguo, marcar conflicto. (Hoy solo hace fecha; el resto es Fase 2+.)

### Pendiente concreto al retomar
1. ~~**Chequeo automático de correctitud** sobre las 273~~ → **HECHO 2026-05-22**, ver addendum abajo.
2. **Medir distribución real de preguntas** (definicional vs relacional) antes de invertir en GraphRAG-relacional.
3. Ampliar detección de glosarios (el builder captó 10 con opener "se entenderá por"; faltan otros formatos — ej. Acometida no quedó).

---

## ADDENDUM 2026-05-22 — Fase 0 cerrada: cifra dura de correctitud

Branch `feat/fase0-correctness` (off main, post-merge PR #4). Implementado el
chequeo de correctitud que el roadmap pedía como criterio de aceptación de
Fase 0. **Cifra dura, no proxy.**

### Qué mide (`src/eval/correctness.py`, funciones puras + 10 tests)
Por cada query in_domain:
- **`cited_expected`** (binario, EXACTO): ¿la respuesta cita el artículo
  definidor esperado `(norma, articulo)`? Sin fuzzy.
- **`definition_recall`** (continuo 0..1): fracción de palabras-contenido de la
  definición de glosario presentes en la respuesta (mide fidelidad a la
  definición curada, no alucinación). Stopwords ES + de-acento + tokens ≥3.

`scripts/score_correctness.py` consume un results JSON y mapea cada query a su
`conceptos.definicion` con la MISMA normalización que el inject. Off_corpus se
puntúa aparte (refusal). Correr: `PYTHONPATH=. ./venv/bin/python
scripts/score_correctness.py --results data/eval/results/<ts>.json`.

### Resultado sobre v3 (273 in_domain, run 20260522T172221Z, HEAD actual)
| métrica | valor |
|---|---|
| **strict_correct** (cita art esperado **Y** def_recall≥0.6) | **85.7%** |
| cited_expected (cita exacta del art esperado) | 87.9% |
| definition_recall media / mediana | 0.94 / 1.0 |
| def_recall ≥ 0.8 | 89.7% |
| in_domain sin mapear a concepto | 0 / 273 |
| **off_corpus refusal** | **100%** ✅ |

**Esta es la cifra honesta de calidad** (recall+art=100% era por construcción).
El sistema responde con el artículo y la definición correctos en ~86% de las
definicionales, sin alucinar en off_corpus.

### El ~12% que falla — 3 fenómenos REALES (no artefacto)
1. **Colisión de entidades** (ej. "Comité"): respondió sobre el Comité de
   autorizaciones sectoriales (norma 1216930) en vez del energético (1146553).
   → confirma la sección A del roadmap (resolución de entidades) como el
   próximo cuello real, NO un bug de retrieval.
2. **Definición duplicada dentro de una norma** (ej. "Suficiencia de Potencia":
   citó art 59, eval esperaba art 13 de la misma 250604). → la regla de
   vigencia hoy desempata entre normas, no entre artículos de la misma norma.
3. **Falso negativo de la métrica** (ej. "Panel"): respuesta correcta pero la
   def de glosario es una referencia cruzada ("Panel de Expertos del Título VI
   del DFL 4/20.018") → token-recall castiga el parafraseo correcto. Limitación
   conocida de `definition_recall`; cited_expected NO se ve afectado.

### Implicación para el roadmap
El próximo techo NO es retrieval (recall+art=100% por inject) ni generación
(def_recall 0.94) — es **resolución de entidades** (fenómeno 1). Eso adelanta
la sección A / precedente de Fase 3 por encima de aliases (Fase 1). Sigue
pendiente medir distribución real de preguntas antes de invertir en relacional.

---

## ADDENDUM 2026-05-23 — Iteración A (nombres canónicos) aplicada + set diverso medido

### A implementada y aplicada (commits 825e6f6 + este)
Regla A determinista (`src/extraction/canonical_names.py`, 13 tests): extrae el
nombre canónico desde la apertura de la definición (verbatim), degrada la
palabra suelta a alias. Runner `scripts/canonicalize_concepts.py` (idempotente).
**Aplicado: 6 renames** (Comisión→Comisión Nacional de Energía, Coordinador→…
independiente del SEN, Panel→Panel de Expertos, Superintendencia→…de Electricidad
y Combustibles, Informe Técnico de Valorización→…de las instalaciones, Registro
de Participación→…ciudadana). **1 a revisión** (Comité, demasiado largo) en
`glossary/incoming/canonical_review.yaml`. Spec/plan: docs/superpowers/{specs,plans}/2026-05-22-canonical-*.

### Set de eval DIVERSO (nuevo instrumento): 182q, 8 categorías
`data/eval/queries_diverse.jsonl` (`scripts/build_eval_diverse.py`) — cada
categoría mide un comportamiento distinto, NO se agregan. Medido antes/después de A.

| categoría | n | recall@5 | cita_ok | answered | refusal |
|---|---|---|---|---|---|
| definicional_canonico | 30 | 100% | 83% | 100% | 0% |
| fraseo_variado | 60 | 97% | 72% | 95% | 5% |
| alias_sigla | 41 | 100% | 63% | 80% | 20% |
| off_domain_corpus | 6 | 100% | n/a* | 83% | 17% |
| off_corpus | 15 | — | — | 0% | **100%** ✅ |
| ambiguo / relacional / natural | 30 | — | — | 100% | 0% |

\* cita_ok de off_domain = artefacto (no se le puso expected_articulo).

### A NO movió la aguja del eval (honesto)
Antes/después: **2 de 182 queries cambiaron, y ninguna era un concepto renombrado
→ ruido puro de Ollama, no efecto de A.** A es higiene de datos correcta pero
**invisible a este eval**: los aliases (CNE/SEC/CEN) ya existían pre-A, y Comité
fue a revisión (no se auto-aplicó). Esto confirma la predicción del spec: el lift
de citas vive en B, no en A. Valor de A = nombres canónicos limpios + la cola
destapa la ambigüedad de "Comité" para B.

### El cuello real (cita_ok 63-83%) se descompone en 3 causas — diagnosticadas
1. **Autoridad (B)**: "qué es SEC" citó la **Ley 18410 (29819)** en vez del
   glosario del Decreto 37 — y la Ley es **MÁS autoritativa** (lex superior).
   → Hallazgo profundo: **nuestro propio ground-truth del eval es ingenuo de
   autoridad** (espera el glosario-decreto), así que "cita_ok" mide contra un
   gold parcialmente equivocado hasta tener B. B arregla el sistema Y el eval.
2. **Atribución intra-norma (C, focused dormido)**: "potencia firme" citó art 49
   en vez del glosario art 13 de la misma 250604.
3. **Rechazo de acrónimos (D + guard)**: SEC/CEN/SEN/VI/PELP **se rechazan** aun
   siendo aliases curados → el inject de alias no entrega para acrónimos cortos
   y/o el guard off-topic es muy agresivo con queries cortas. 9/41 alias refusados.

### Ambigüedad nunca marcada (confirma B3)
El sistema elige uno calladamente, a veces del dominio equivocado: "qué es Comité"
→ Comité de autorizaciones (no energético); "qué es el Ministerio" → Ministerio de
Desarrollo Social (!). Motiva la UX de ambigüedad (mostrar opciones).

### Relacional irregular (dimensiona G/GraphRAG)
A veces contrasta ambos conceptos, a veces define solo uno. No traversa confiable,
pero NO está roto — retrieval+LLM improvisa comparaciones cuando ambos caen en el
pool. GraphRAG lo haría confiable; sin él es hit-or-miss.

### Próximo paso
B (resolución por autoridad) es ahora el lever claro Y además **corregiría el
ground-truth del eval**. Bases ya verificadas y en memoria
([[reference_chilean_norm_hierarchy]]): rango desde título (no `tipo`), derogación
CC 52-53, vinculaciones BCN vacías. B necesita su propio spec.
Resultados crudos: `/tmp/baseline_diverse.json`, `/tmp/after_diverse.json`.

### ADDENDUM 2026-05-23b — bug de acrónimos: causa raíz + fix (cerrado)
Debugging sistemático del rechazo de acrónimos (SEC/CEN/SEN…).
**Causa raíz** (confirmada, no el guard ni retrieval): al preguntar por un
**alias/sigla**, el inject resuelve y pone el artículo al tope, pero ese artículo
define solo la **expansión** — el token literal "SEC" NO está en el texto. El LLM,
bajo grounding estricto, no puede anclar "SEC" y rechaza (no-determinista: "VI"/
"CEN" a veces puenteaban, SEC/SEN no).
**Fix** (`src/pipelines/concept_injection.py`): rama **alias-aware** en
`inject_definition` — cuando el término matcheado ≠ nombre canónico, inyecta un
chunk que dice el vínculo explícito «alias» se refiere a «canónico». {def}
(verbatim, datos curados). Scoped a queries-por-alias → **sin riesgo de las
regresiones de colisión** (esas eran queries por nombre canónico). `_concept_index`
y `find_curated_definition` ahora cargan el nombre canónico (4-tupla).
**Resultado** (categoría `alias_sigla`, 41q): answered 78→**97%**, cita_ok
63→**95%**, 8 recuperadas, **0 regresiones**. 8 tests unitarios nuevos/actualizados.
Nota: queda 1 test stale pre-existente (`test_generate_passes_response_format`,
del constrained-decoding desactivado) — limpieza aparte.
