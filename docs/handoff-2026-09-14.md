# Handoff — 2026-09-14 (cierre)

Punto de entrada para la proxima sesion. Reemplaza al handoff del 2026-08-24.

---

## 1. Estado en una tabla

| | dev (114) | held-out (64) |
|---|---|---|
| Config vigente | quote-first + **quote-only, tope 2 citas** | |
| cita_ok (encuentra el articulo correcto) | **80/114** (70 %) | **62/64** (97 %) |
| cita_limpia (todas las citas correctas) | **80/114** | **58/64** |
| precision de citas | 0.51 | **0.89** |
| respuestas en prosa (caen fuera de quote-only) | 11/114 | **11/64 (17 %)** |
| latencia mediana de generacion | 19 s | 35 s |
| fiel_estricto con quote-first (#71) | 43 % | **67 %** |

**Veredicto de producto (#68, sin cambios): SOLO BUSCADOR.** El criterio pedia fiel_estricto >= 80 %
y el mejor valor medido es 67 %. Quote-only convierte al sistema en "buscador con evidencia": entrega
frases textuales verificadas con su articulo. En esas respuestas la fidelidad es 100 % por construccion.

**Caveat que domina todo:** las 178 preguntas del eval las escribio el asistente mirando el corpus.
Los dos sets difieren mucho de base (cita_limpia dev 40 % vs held-out 73 % antes de quote-only).

## 2. Config vigente (`src/core/config.py`)

```
answer_think              True    exp #63
self_consistency_n        3       exp #54
answer_quote_first        True    exp #71   fiel_estricto 39 -> 67 held-out
answer_quote_only         True    exp #76   no redacta si alguna cita verifica
answer_quote_max          2       exp #76   con 8 rociaba; con 1 perdia golds
answer_quote_reatribuir   False   exp #77   EN MEDICION, ver seccion 4
embed_4b_num_ctx          4096    RAM del embedder 9.8 GB -> 3.7 GB, vectores identicos
glossary_lookup           True    exp #43/#44
ambiguity_disclose        True    exp #50
filtrar_fuera_dominio     True    exp #52
```

## 3. Lo que se hizo en esta etapa (06-09 a 14-09)

- **#68 fidelidad**: primera medicion de si la prosa dice lo que dice el articulo. 29 % dev / 39 % held-out.
- **#69b reparacion de DB**: el parser cortaba articulos en las notas BCN. Parser arreglado; DB reparada
  (511 updates, 281 fantasmas, 218 nuevos, v2 con el fix de 18 textos inalcanzables). Reversible.
- **#71 quote-first ADOPTADO**, **#76 quote-only tope 2 ADOPTADO**.
- **Rechazados**: #70 prompt sin meta (sobreajuste a dev), #72 filtro por juez (borra el gold),
  #74 modelo denso 27B (1369 s/pregunta), #75 quote-only tope 8 (rociado).
- **Diagnostico del 17 % en prosa (9/9)**: el modelo copia bien la frase y la ETIQUETA mal. Toma la
  referencia de una nota BCN pegada en el articulo (`[Art. primero N° 8, d)]`) o inventa el numero.
  7 etiqueta invalida, 1 norma inexistente, 1 extractor vacio (definicion partida por una nota).
- **Memoria**: `qwen3-embedding:4b` cargaba con ctx 32768 (9.8 GB RAM en CPU) y el sistema mataba
  corridas. `embed_4b_num_ctx=4096` adoptado con criterio fijado antes (coseno 1.000000 en 40/40).
- **GPU**: incidente Xid 79 el 13-09. Guardrails nuevos (seccion 6).
- **Calibracion del juez**: el juez 30b abusa del PARCIAL (~46 % mal marcados; el 27B baja PARCIAL a 3 %).

## 4. En curso al cerrar

- **`reatr_holdout`** (cola v28, `scripts/plan_maestro.txt`): mide #77 en held-out. Iba 10/64 a las 13:25,
  termina ~14:25. Se relanza sola si algo muere.
- **#77 en dev ya dio**: cita_ok 80 -> 80, cita_limpia 80 -> **79**, prosa 11 -> **11**.
  Por la letra del criterio ("cita_limpia no cae") **#77 ya falla en dev** y NO se adopta. El held-out
  sirve para saber si el mecanismo rescata los casos del glosario; no revierte el veredicto.
- Para leer el held-out:
  ```bash
  cd /home/alonso/Documentos/Github/energy-rag-postgres-rag
  env PYTHONPATH=. venv/bin/python -m scripts.comparar_corridas data/eval/results/qonly2_holdout data/eval/results/reatr_holdout
  ```

## 5. Que mas se puede hacer, en orden

| # | que | por que | costo |
|---|---|---|---|
| 1 | **20-40 preguntas reales del usuario** | todo el eval es propio; quote-only necesita medir si se ENTIENDE | 30 min del usuario |
| 2 | **Limpiar notas BCN de 598 articulos** (19 % del dominio) | causa raiz de las etiquetas mal atribuidas, del extractor vacio y del juez | refragmentar + re-embeber, GPU |
| 3 | **Obligaciones**: extractor incremental + arreglar filtro + sumarlo al monitor | aplicar un cambio borra obligaciones y nadie las re-extrae: LGSE 862 -> 0, LEY 20936 227 -> 0 | 515 articulos x ~6 s = ~52 min GPU |
| 4 | **Definiciones**: indice unico + constructor incremental | 84 de 100 glosarios sin fragmentos: sus terminos no existen para el diccionario; re-correr duplica (6 duplicados hoy) | bajo |
| 5 | **Articulos > 6000 chars** | el extractor nunca los lee: 73 del dominio, largo medio 12605 | chunking por incisos |
| 6 | **Skills del proyecto no cargan** | viven en este worktree; Claude se abre en `energy-rag` (rama `main`, sin `.claude/skills`) | enlazar carpeta |
| 7 | **Re-embeber el corpus de una vez** | vectores de junio difieren 0.998 de los de hoy; consultas en CPU vs corpus en GPU (~0.9975) | GPU, horas |
| 8 | **17 tests rotos preexistentes** | heredan defaults de produccion (self_consistency_n=3), DB de test sin `derogado`, modelos offline | bajo |
| 9 | **Normas nuevas** | descubrimiento manual; 142 citadas faltantes (DECRETO 42 citado 77 veces); crawlers CNE/SEC/CEN no existen | medio |
| 10 | `build_def_fragments.py` y `embed_4b.py` sin `num_ctx` | no heredan el arreglo de memoria | trivial |

Filtro del extractor de obligaciones con bug: "tendran que" y "comunicara" nunca calzan (salto de linea
pegado dentro del SQL). 3 normas quedaron sin articulos.

## 6. Operacion

**Cola:** `scripts/plan_maestro.txt` -> `trabajar.sh` / `runner.sh` (cron). Pausa: `touch .watchdog_off`.

**GPU (RTX 3090):** falla de hardware recurrente, **seis Xid 79** (05-08, 07-08, 11-08, 14-08, 08-09,
13-09). Dos sin carga y una a 180 W: no es potencia ni calor. Pendiente revision fisica: un cable PCIe
por conector, fuente, asiento, BIOS X870 F8 (probar PCIe Gen3).

| pieza | que hace |
|---|---|
| `scripts/gpu_guard.sh` | antes de lanzar: bloquea si el arranque anterior termino en Xid 79, si nvidia-smi no responde, o GPU >= 80 C; reaplica `.gpu_limite` |
| `scripts/gpu_vigia.sh` | cron cada minuto: corta la carga ante Xid, GPU muerta, generador en CPU o GPU >= 88 C x3 |
| `.gpu_bloqueo` | solo una persona lo borra |
| `scripts/gpu_modo.sh <W>` | cambia el tope y lo guarda en `.gpu_limite` (hoy 180 W; 230 W da la velocidad de 350 W) |

Diagnostico de caidas: `journalctl -k -b -1`, nunca `dmesg`. Protocolo: skill `gpu-seguridad`.

**Seguridad de Claude Code (estado real):** cero reglas deny, cero sandbox, sudo sin contrasena solo para
`nvidia-smi`. Un hook `PreToolUse` NO es un candado: solo bloquea con exit code 2 y falla abierto con otros
errores. Lo mas fuerte es el sandbox (bwrap y socat instalados; probar que no corte Ollama/Docker).

**Plugins:** `ponytail@ponytail` v4.10.0 instalado y activo (requiere reiniciar Claude Code).

## 7. Git

- Se trabaja en el worktree `energy-rag-postgres-rag`, rama local `adopt-winners`, y se sube con
  `git push origin HEAD:main`. `main` == HEAD al cerrar.
- `origin/adopt-winners` tiene 22 commits del 01-08 al 07-08 que no estan en HEAD: son copias de antes de
  la reescritura de historia; los 22 asuntos existen en `main`. Respaldo en `backup-antes-limpieza`.

## 8. Trampas nuevas (sumar a las del handoff anterior)

```
juez = mismo modelo que responde       abusa del PARCIAL; calibrar antes de creerle
reversibilidad nunca ejecutada          --revertir fallaba por columnas GENERATED
diagnostico en el arranque actual       dmesg no ve la caida; journalctl -k -b -1 y todos los arranques
tunear y validar en el mismo set        el valor se elige en dev y se decide en held-out una vez
metrica que premia decir menos          cita_limpia; descartado con guarda (tope 1 empeora)
referencia mezclada en un test          vectores de junio vs septiembre y CPU vs GPU
skills en el worktree equivocado        el proyecto las da por cargadas y nunca lo estuvieron
```

## 9. HUECO: nada avisa cuando algo falla

Hoy las fallas quedan **registradas pero no notificadas**:

| que falla | donde queda | aviso al usuario |
|---|---|---|
| GPU cae (Xid 79), nvidia-smi muere, modelo a CPU, GPU caliente | `.gpu_bloqueo` + `logs/gpu_vigia.log` (`CORTE`) | ninguno |
| la guarda no deja lanzar | `logs/gpu_guard.log` | ninguno |
| un experimento falla | `logs/runner.log` (`FALLO`, `FALLO DEFINITIVO`) | ninguno |
| la cola se vacia | `logs/watchdog.log` (`PLAN AGOTADO`) | ninguno |

Revisar a mano al volver:
```bash
cd /home/alonso/Documentos/Github/energy-rag-postgres-rag
cat .gpu_bloqueo 2>/dev/null; tail -3 logs/runner.log; grep CORTE logs/gpu_vigia.log | tail -2
```

**Pendiente, primera tarea de la proxima sesion:** un `scripts/avisar.sh` llamado desde `cortar()` del vigia,
`bloquear()` de la guarda y las ramas `FALLO` del runner.
- Escritorio (`notify-send`, local): desde cron necesita `DISPLAY` y `DBUS_SESSION_BUS_ADDRESS`.
- Celular (ntfy, Telegram, correo): manda datos a un servicio externo; decision del usuario.
- **No se hizo al cerrar a proposito:** habia un experimento corriendo y `runner.sh` estaba en ejecucion.
  Editarlo en sitio puede romper el registro de esa corrida; hay que reemplazar los scripts con `mv`
  atomico, no reescribirlos.
