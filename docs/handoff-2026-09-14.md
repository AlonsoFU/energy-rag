# Handoff — 2026-09-14

Reemplaza a `handoff-2026-08-24.md` como punto de entrada.

## Qué cambió desde agosto, en una tabla

| | antes | ahora |
|---|---|---|
| Respuesta | prosa redactada por el LLM | **citas textuales verificadas** (quote-only, tope 2) |
| fiel_estricto held-out (#68 → #71) | 39 % | 67 % con quote_first; quote-only es 100 % por construcción en el 83 % de respuestas en formato cita |
| cita_limpia held-out | 47/64 | **58/64** |
| precisión held-out | 0.71 | **0.89** |
| Latencia mediana | ~220 s | **19-35 s** |
| DB | 338 artículos basura por notas BCN | reparada (#69b-v2), reversible |
| GPU | sin guardas | guarda + vigía + bloqueo (6 caídas Xid 79) |

## Config vigente (`src/core/config.py`)

```
answer_think         True    exp #63
self_consistency_n   3       exp #54
answer_quote_first   True    exp #71   primera mejora real de fidelidad
answer_quote_only    True    exp #76   no redacta si alguna cita verifica
answer_quote_max     2       exp #76   con 8 rociaba, con 1 perdía golds
```

## Lo que NO está resuelto

1. **Las preguntas reales del usuario.** Las 178 del eval las escribió el asistente mirando el
   corpus. Con quote-only el riesgo nuevo es la legibilidad, y eso ningún número interno lo mide.
2. **17 % de las respuestas del held-out caen a prosa** (ninguna cita verifica). Ahí sigue el
   riesgo de parafrasear mal.
3. **Hardware de la GPU.** Seis Xid 79 desde el 05-08, dos sin carga, una a 180 W. No es
   potencia. Revisar cables PCIe (uno por conector), fuente, asiento, BIOS X870 F8.
   Protocolo: `.claude/skills/gpu-seguridad/SKILL.md`. Operación: `docs/manual-operacion.md`.

4. **17 tests fallan, TODOS de antes de esta etapa.** Verificado por nombre contra el commit
   `df97291` (18 fallos ahí) y contra la config de ayer (los mismos 17): la adopción de #71/#76
   no rompió ninguno, y arregló `test_generate_answer_accumulates_tokens` de rebote. Grupos:
   - 4 en `test_generate.py`: cuentan llamadas al LLM y no fijan `self_consistency_n`; con el
     default n=3 (exp #54) hay 5 llamadas donde esperan 2. Con `SELF_CONSISTENCY_N=1` pasan.
   - DB de test sin la columna `derogado` (`test_vectorstore`, `test_retrieve`, e2e).
   - embedder y reranker: el modelo no está en el caché por defecto con `HF_HUB_OFFLINE=1`.
   - resto: `test_llm`, `test_cli`, `test_definition_source`, `test_concept_injection_subject`,
     `test_date_extraction_realworld`.
   Arreglarlos es trabajo aparte: los tests deberían fijar sus propios settings en vez de
   heredar los defaults de producción.

## Rechazados esta etapa (no reintentar sin idea nueva)

```
#70 prompt sin meta       +19 dev, -3 held-out: sobreajuste al set donde se afinó el #50
#72 filtrar por juez      borra el 48 % de las frases y se lleva el gold
#74 modelo denso 27B      1369 s/pregunta, 8x el MoE
#75 quote-only tope 8     rociaba: 3.45 citas/resp
```

## Trampas nuevas (sumar a las del handoff anterior)

```
juez = mismo modelo que responde   abusa del PARCIAL (~46 % mal marcados, 27B lo baja a 3 %)
reversibilidad nunca ejecutada     --revertir fallaba por columnas GENERATED; se probó recién al usarlo
diagnóstico en el arranque actual  dmesg no ve la caída; mirar journalctl -k -b -1 y todos los arranques
tunear y validar en el mismo set   el valor se elige en dev, se decide en held-out una sola vez
métrica que premia decir menos     cita_limpia; se descartó con guarda (tope 1 empeora)
```
