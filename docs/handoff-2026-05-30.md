# Handoff — Energy-RAG (2026-05-30)

> Documento de cierre de la sesión 2026-05-29/30. Autosuficiente para retomar.
> Doc previo: `docs/handoff-2026-05-20.md` (addenda hasta 2026-05-29).
> Rama de trabajo: `feat/definition-source-resolver` (NO mergeada).

## TL;DR
El sistema es un buen **diccionario** (preguntás el término → da la definición, 83%)
pero **débil como buscador conceptual** (describís la situación → no llega, 33%).
**Diagnosticado**: 15/16 fallos son RETRIEVAL (el artículo correcto ni llega al pool),
no generación. El cuello fino: la inyección por concepto YA lo resolvería, pero no detecta
el concepto cuando la query lo PARAFRASEA en vez de nombrarlo. El frente es retrieval/
detección-de-concepto, NO el anchor ni las aristas.

## NÚMEROS (dos sets, leer la diferencia)
- **Set plantilla `queries_diverse` (248q, gold derivado de la curación)**: GLOBAL **91%**,
  truth 90%. **INFLADO** — mide consistencia con la propia curación (gold circular).
- **Set INDEPENDIENTE `queries_independent.jsonl` (49q, gold LEÍDO de la ley)**: **64%** con
  gold (28/44). Es el número HONESTO. Desglose:
  | categoría | cita_ok |
  |---|---|
  | indep_def (definiciones directas, fáciles/medias) | **83%** (20/24) |
  | indep_complex (situacionales/paráfrasis) | **33%** (5/15) |
  | indep_gral_detalle (trampas AVI, gold=reglamento) | 33% (1/3) |
  | indep_alias | 100% (2/2) |
  | off_corpus refusal | 3/3 ✓ |
  Baseline guardado: `data/eval/results/indep49_baseline.json` (anchor ON + guard article-level).

## QUÉ SE HIZO ESTA SESIÓN (todo commiteado en la rama)
1. **Cadena LGSE Pasos 2-5** (commits previos): aristas define_termino en la LGSE
   (glossary_define_edges, curate_lgse_glossary), autoridad por jerarquía chilena,
   remisiones por grafo (follow_remissions). Doc en handoff-2026-05-20 addenda.
2. **BUG GRANDE de 4 capas — sufijo "Nº-M"** (commits `2f1bd7b`, `8291945`): la ingesta
   perdía 66 arts (~20% de la LGSE: título completo del Coordinador 72-1…72-22, 212-1…,
   quinquies) porque "72°-1"→"72" colisionaba y se dedupeaba. + follow_remissions truncaba
   la ref + CITATION_PATTERN no parseaba "212-1". TODO arreglado. Re-ingesta: 264→**330 arts**.
   Lo cazó un **set independiente** (gold leído a mano) — no el set plantilla.
3. **Anchor + guard article-level** (commits `796cb3a`, `a9a9cda`): anclaje determinista de
   cita (sigue la arista define_termino del concepto a su artículo A; si el LLM no citó A, lo
   anexa). Guard afinado a "article-level" (omitir solo si citó A exacto, no toda la norma) →
   arregla atribución intra-norma (glosario art vs hermano operativo).
   - Flags en config: `anchor_authoritative_citation` (default OFF), `anchor_guard_exact_article`
     (default OFF). **Validados pero NO activados por default — decisión pendiente.**
   - Medido en set independiente: norma-level 80% → article-level 90% (set chico),
     61→72% (set 23q). Las trampas AVI **sobreviven** (el guard NO sobre-corrige). +legítimo.
4. **Banco independiente de eval** (commits `956029f`, `12b4971`): `queries_independent.jsonl`,
   49 queries, gold verificado leyendo la ley (que el término aparezca en el artículo). Mezcla
   fácil/compleja. Reemplaza al plantilla como fuente de número honesto.

## EL FRENTE REAL (los 16 fallos del set independiente, por patrón)
1. **Retrieval no entiende paráfrasis (mayoría):** "una central que genera para su consumo…"
   no encuentra "autoproductor"; "el ente que resuelve discrepancias…" devuelve vacío; "qué
   pasa con el voltaje cuando una subestación lo baja…" vacío. El sistema necesita que la query
   NOMBRE el término; si lo describe, falla. → **es comprensión/retrieval, no anchor ni aristas.**
2. **Rechazos en complejas:** varias citaron `[]` (el LLM no ancla cuando la query es indirecta).
3. **General-vs-detalle:** "fórmula del VATT/VI" cita la LEY (258171/103) cuando el gold es el
   REGLAMENTO del método (1146553/48). El sistema sobre-prefiere la ley.
4. **Ambigüedad de norma:** "energía firme en el reglamento de transferencias" cita la LGSE
   (que también la define) en vez de 250604/13.

## DIAGNÓSTICO de los 16 fallos: 15 son RETRIEVAL, 1 generación (medido)
Partido por `retrieval_hit` (gold norma+art en el pool top-5) del baseline indep49:
- **A) 15/16 = el gold NI LLEGÓ al pool** → problema de **RETRIEVAL**, no de generación.
- B) 1/16 = estaba en el pool y no se citó ("energía firme" citó la LGSE en vez de 250604/13).

Matiz CLAVE dentro de A: en **12 de 15 la NORMA sí llega** (`retrieval_norma_only_hit=True`)
— el artículo correcto está en la norma correcta pero **pierde el top-5 contra artículos
HERMANOS de la misma norma**. La paráfrasis ("una central que genera para su consumo…")
matchea débil contra el artículo que la define (art 225 glosario / art 79 acceso abierto) y
gana otro chunk. → El cuello es la **granularidad/ranking intra-norma con queries
parafraseadas**, no que falte el documento.

**Implicación**: el fix es RETRIEVAL. La maquinaria de inyección por concepto
(`inject_definition` / `find_subject_concept`) YA resolvería esto **si detectara el concepto**
— pero hoy busca el NOMBRE/alias exacto en la query y la paráfrasis no lo nombra. Ese es el
cuello fino: **detección de concepto desde una descripción** (no desde el nombre literal).
Sin verificar aún: ¿`find_subject_concept` detecta el concepto en estas 15? (chequeo CPU
de 10 min, próximo).

## PASO 1 — HECHO (2026-05-31): el cuello fino, confirmado y REFINADO
Corrido `scripts/diag_concept_detection.py` (solo-lectura) sobre las 15 `indep_complex`:
- **detecta Y acierta: 0/15.** Prender `inject` para complejas NO arregla nada.
- **NO detecta: 7/15** — la query describe sin nombrar el término (mapeo descripción→concepto ausente).
- **DETECTA pero artículo MAL: 8/15** — HALLAZGO NUEVO: la query SÍ contiene un término literal,
  pero es el **CONTEXTO, no el sujeto**. Ej: "cómo se reparte la plata dentro de un *sistema de
  transmisión*" → detecta "Sistema de Transmisión" (art 73) cuando el sujeto es *repartición de
  ingresos* (art 117). "el coordinador tiene que vigilar competencia" → detecta el def del
  Coordinador (212-1) en vez del art operativo 72-10.
- **Implicación correctora del Paso 2:** inyectar el artículo del concepto detectado tal cual
  **DAÑARÍA 8/15** (inyecta el artículo paraguas equivocado, lo pone score 9999 al tope). La
  maquinaria de inject sirve para "qué es X" (X = sujeto), NO para situacionales (término = escenario).
  El fix real necesita un paso de COMPRENSIÓN descripción→sujeto (difuso/LLM, lane legal-safe),
  no un flip de flag. NO prender `inject_curated_definitions` para esta clase de query.

## PASO 2 — EXPLORADO (2026-05-31): resolver LLM descripción→sujeto = 4/15, techo 8/15
- **Techo de la vía inject**: solo **8/15** golds son artículo-definición de algún concepto curado
  (`scripts/diag_concept_detection.py` reverse-index). Los otros 7 (212 panel, 117 repartición,
  72-10 monitoreo, 147 cliente libre, 99 obras, 162 precios nudo, 72-11 cadena pagos) son
  artículos OPERATIVOS, no glosario → ningún resolver concepto→definición los alcanza. **A y B
  son complementarios**: A cubre ≤8, los otros 7 necesitan ranking intra-norma (vía B).
- **Resolver LLM** (`scripts/diag_llm_subject_resolver.py`, qwen3.5:9b, le da la lista de 371
  nombres y pide el SUJETO): **4/15**. Acierta sujeto-claro (SEC, Acceso Abierto, cogeneración,
  transmisión-segmentos). Falla 11 **con confianza**, eligiendo conceptos HERMANOS plausibles
  (ej. "Planificación de la Transmisión"/art 87 vs gold "Planificación Energética"/art 83).
- **Veredicto**: inyectar la elección del resolver a ciegas (score 9999 al tope) ayudaría 4 y
  **dañaría hasta 11**. NO ship-ready. El diseño correcto = **resolver + CORROBORACIÓN de
  retrieval**: solo promover el (norma,art) propuesto si el pool YA lo trajo (gate de confianza)
  → convierte "forzar el equivocado al tope" en "subir el correcto cuando está corroborado".
  Difuso para ENTENDER, exacto para CITAR, y el gate evita el daño. Implementación + eval
  dedicada (no flip de flag). Es el Paso 2 refinado.

## PRÓXIMOS PASOS (en orden)
1. ~~Confirmar el cuello fino~~ HECHO. ~~Explorar resolver~~ HECHO (ver arriba).
1b. **Paso 2 refinado**: implementar resolver+corroboración (gate: inyectar/rerank solo si
   retrieval ya trajo el (norma,art) que propone el resolver). Cubre ≤8/15. Para los otros 7,
   ranking intra-norma. Pendiente — es la próxima sesión de implementación.
2. **Atacar el retrieval de paráfrasis** según (1): probablemente detección de concepto desde
   descripción → inyectar el artículo del concepto al pool. CUIDADO split legal: difuso para
   ENTENDER la pregunta (mapear descripción→concepto), exacto/curado para la CITA. NO subir
   solo top-k a ciegas (solo ayuda en los 12 con norma_hit, y dispersa).
3. **Decidir flags anchor por default** (`anchor_authoritative_citation` +
   `anchor_guard_exact_article`): validados, ayudan, sin trade-off medido. Decisión de producto
   (anexan línea de fuente al texto). OJO: el anchor NO arregla esto — es retrieval, no cita.
4. **Consolidar: PR/merge** de `feat/definition-source-resolver` (mucho trabajo sin mergear).
5. (menor) dedup singular/plural de conceptos ("Sistema(s) de Transmisión Zonal", id 410 vs 275).

## GOTCHAS DE INFRA (aprendidos a la mala esta sesión)
- **Postgres `energy_rag_pg` (Docker, puerto 5434) se cae solo** → un eval contra DB caída da
  **0% FALSO** (n_docs=0 en todo). SIEMPRE `docker start energy_rag_pg` + un SELECT de sanidad
  ANTES de un eval largo. Reiniciar NO pierde datos (volumen persiste).
- **RAM disputada por `ragflow-es-01`** (Elasticsearch de RAGFlow, contenedor Docker, ~2.6GB):
  `docker stop ragflow-es-01` para liberar; `docker start` para recuperar. (RAGFlow = otro
  proyecto; apagar ≠ eliminar, es reversible.)
- **NO usar `until ! pgrep -f "src eval"` como waiter**: el propio comando del waiter contiene
  "src eval" → el pgrep se auto-matchea → DEADLOCK eterno (quedaron waiters de 25h colgados).
  Esperar por el background-task del eval directamente, o por PID/archivo de salida.
- Eval ~28s/query (qwen3.5:9b, cuelga parcial hasta 56s en algunas pero completa).

## ESTADO DE DATOS (DB de producción)
- LGSE (258171) = **330 arts** (no 264), 60 aristas define_termino, con 72-1…/212-1/quinquies.
- Conceptos: +34 del glosario LGSE curados. Aristas/metadata re-generables con los scripts.
- Set de eval honesto: `data/eval/queries_independent.jsonl` (49q).
