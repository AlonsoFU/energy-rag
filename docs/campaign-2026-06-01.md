# Campaña de experimentación — 2026-06-01 (rama feat/definition-source-resolver)

Objetivo: probar todos los levers de retrieval y ver cuál GENERALIZA, usando un
set held-out NUEVO (`queries_holdout.jsonl`, gold leído de la ley, artículos
distintos a los de desarrollo) además del dev (`queries_independent.jsonl`).

Métrica: gold∈pool@5/10/20 (retrieval-only, SimpleRetriever, determinista).
Harness: `scripts/campaign_sweep.py`. Resultados: `data/eval/results/campaign/`.

## Hallazgo estructural
El `rerank` con `top_rerank=10` CORTA el pool a 10 ANTES de graph_boost →
el gold en rango 11-20 se descarta antes de poder promoverlo. Reranker = Identity
(no reordena, solo trunca). Por eso @10==@20 en baseline.

## Resultados (gold∈pool@5)
| config | dev @5 (n=44) | holdout @5 (n=18) | veredicto |
|---|---|---|---|
| E0 baseline | 25 | 15 | — |
| E1 rr50 (top_rerank=50) | 25 | 14 | habilita @20 (dev 26→33) pero NO @5 |
| E2 gba (graph_boost_all) | 25 | 15 | nulo solo (pool cortado a 10 antes) |
| E3 rr50+gba | 29 (+4 def) | 13 (−2) | **OVERFIT: dev sube, holdout BAJA** |

## Conclusiones parciales
1. **`graph_boost_all` NO generaliza**: +4 dev / −2 holdout. Promueve el
   artículo-definición del concepto aunque no sea el gold (situacional/operativo).
   El held-out lo cazó. NO adoptar.
2. **`top_rerank` ampliado** habilita profundidad (@20) pero no mejora @5 solo
   (Identity no reordena). Es enabler, no lever.
3. **La clase situacional (gold = artículo OPERATIVO) no la destapa ningún lever
   de grafo/boost** a top_k=5: el gold llega al pool (@20) pero nada legítimo lo
   sube a top-5 desde una paráfrasis. Candidatos restantes: cross-encoder reranker
   (retrieval-only, BGE), o subir top_k de generación (5→10) y dejar que el LLM elija.

## RESULTADO DECISIVO (gold∈pool@5, dev n=44 / holdout n=18)
| config | dev @5 | dev cmplx | holdout @5 | hold cmplx | veredicto |
|---|---|---|---|---|---|
| E0 baseline | 25 | 4 | 15 | 6 | — |
| E1 rr50 | 25 | 4 | 14 | 6 | enabler, no lever |
| E5/E6 HyDE(+rr50) | 29 | 6 | **12** | **3** | **OVERFIT: holdout BAJA, situacional a la mitad** |
| E7/E8 pool100(+rr50) | 25 | 4 | 15 | 6 | nulo |
| E3 graph_boost_all | 29 | 4 | 13 | 5 | OVERFIT |
| **E4 BGE cross-encoder** | **33** | **9** | **17** | **8** | **GANA AMBOS + destapa situacional** |

### Dos vuelcos que el held-out reveló
1. **HyDE PERJUDICA la generalización**: dev +4 pero holdout 15→12 y situacional 6→3 (mitad).
   En sesiones previas se dio como "lever real" mirando solo dev (gold∈pool@20). Con set held-out
   es OVERFIT: los párrafos hipotéticos ayudan a queries vistas y desvían en las nuevas.
   **`hyde_in_simple` debe quedar OFF.** [[feedback_eval_iteration_lessons]]
2. **BGE cross-encoder es el ganador real**: dev 25→33 (+8, +18pp), holdout 15→17 (+2), resuelve
   la situacional (dev complex 4→9, holdout 6→8). El rechazo histórico de BGE era por *grounding*
   (citaba menos canónico), NO por *recall@5*. Para retrieval, generaliza. Implementado
   retrieval-only en `scripts/campaign_sweep.py` (BGEReranker, CPU: Pascal sm_61 sin kernels GPU).

## FASE GENERACIÓN — VEREDICTO (holdout, top_k=5, AdaptiveRetriever+generate)
| | baseline (BGE off) | BGE on (rr30) |
|---|---|---|
| cita_ok | 14/18 | **15/18 (+1)** |
| └ hold_complex (situacional) | 5/9 | **6/9 (+1)** |
| └ hold_def | 9/9 | 9/9 |
| grounding_pass | 18/18 | **18/18** |
| answered | 18/18 | 18/18 |
| negativas refused_ok | 4/6 | 4/6 |

- **BGE sostiene/mejora cita_ok (+1) SIN matar el grounding (18/18)**. El temor histórico (BGE
  baja el grounding del LLM) NO se materializa usando BGE como reranker de RETRIEVAL (reordena el
  pool; la verificación de citas sigue contra el pool). Sin regresión.
- El delta de generación (+1) < el de retrieval (+2 holdout / +8 dev): la rama compleja del
  Adaptive ya recupera algo y top_k=5 lo limita. La señal de retrieval (62q) es la evidencia
  fuerte; generación confirma "no hace daño + mejora leve".
- **VEREDICTO: BGE es ADOPTABLE** (use_bge_reranker + top_rerank_override=30), flag-gated en
  producción (`src/components/reranker.py:get_reranker`, default OFF). Pendiente: confirmar en
  dev 44q (señal más fuerte) y decidir top_k 5 vs 10.

## CONFIRMACIÓN DEV 44q (generación, top_k=5) — VEREDICTO FINAL
| | baseline | BGE (rr30) | Δ |
|---|---|---|---|
| cita_ok | 25/44 | **30/44** | **+5 (+11pp)** |
| └ indep_complex (situacional) | 5/15 | 7/15 | +2 |
| └ indep_def | 17/24 | 20/24 | +3 |
| └ indep_gral_detalle | 1/3 | 1/3 | = |
| grounded | 42/44 | **43/44** | +1 |
| answered | 41/44 | **43/44** | +2 |
| negativas refused_ok | 3/5 | 3/5 | = |

**Consolidado (dev + holdout, retrieval + generación):**
- recall@5: dev 25→33, holdout 15→17.
- cita_ok generación: dev 25→30 (+5), holdout 14→15 (+1).
- grounding: NO baja (dev 42→43, holdout 18→18). answered sube. 0 regresión en ninguna métrica/set.

### RECOMENDACIÓN FORMAL: adoptar BGE
Activar `use_bge_reranker=True` + `top_rerank_override=30`. Evidencia consistente en 2 sets
independientes y en retrieval + generación; el riesgo histórico (grounding) NO se materializa.
**Único costo real**: latencia — BGE corre en CPU (Pascal sm_61 sin kernels GPU) → ~+seg por query.
Decisión de producto. Opción de mitigación si la latencia molesta: aplicar BGE solo a la rama
COMPLEJA (situacional, donde más aporta), dejando la simple/definicional rápida — pero def también
ganó (+3 dev), así que el all-queries rinde más. PR/merge de `feat/definition-source-resolver`
recomendado (mucho trabajo acumulado sin mergear).

## BONUS top_k=10 + BGE — CONFIG GANADORA (ambos levers se suman)
| | baseline k5 | BGE k5 | **BGE k10** |
|---|---|---|---|
| dev cita_ok | 25/44 | 30/44 | **32/44 (+16pp)** |
| dev indep_complex (situacional) | 5/15 | 7/15 | **9/15** |
| holdout cita_ok | 14/18 | 15/18 | **17/18** |
| holdout hold_complex | 5/9 | 6/9 | **8/9** |
| grounding (dev / holdout) | 42 / 18 | 43 / 18 | **43 / 18 (intacto)** |
| answered dev | 41 | 43 | **43** |

- **top_k=10 + BGE es la mejor config.** Refuta el viejo temor "top-10 dispersa las citas":
  cuando el top-10 viene BIEN ORDENADO por el cross-encoder, el LLM cita mejor, SIN perder
  grounding (43/44 dev, 18/18 holdout). Los dos levers se SUMAN (recall del reranker + más
  contexto al generar).

## RECOMENDACIÓN FINAL DE LA CAMPAÑA
**Adoptar: `use_bge_reranker=True` + `top_rerank_override=30` + top_k=10.**
- Ganancia consolidada (2 sets independientes): cita_ok dev 25→32 (+16pp), holdout 14→17;
  clase situacional dev 5→9, holdout 5→8; grounding intacto; answered sube. 0 regresión.
- **Único costo**: latencia — BGE en CPU (Pascal sm_61 sin kernels GPU), ~+seg/query. Decisión
  de producto. Mitigación posible: BGE solo en rama compleja (pero def también gana).
- Descartados por el set held-out (overfit): HyDE, graph_boost_all. Nulos: pool_depth.
- **Acción**: PR/merge de `feat/definition-source-resolver` (incluye toda la cadena LGSE +
  330 arts + anchor + BGE wiring). Flags quedan OFF por default hasta decisión de producto.

## Wiring de producción (commiteado, default OFF)
- `use_bge_reranker` + `get_reranker()` factory; cli.py usa la factory (query + eval).
- `top_rerank_override` para que BGE rerankee un pool suficiente (~30). BGE en CPU (Pascal sm_61).
- top_k=10 se pasa al CLI con `--top-k 10` (default actual 5).
