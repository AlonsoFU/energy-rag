# Handoff — 2026-08-26

Reemplaza a `handoff-2026-08-24.md`. Con leer esto alcanza.

## Estado

| | |
|---|---|
| Corpus | **122 normas** (76 en dominio) · 4526 artículos · 21 derogados · 68 duplicados |
| Foso | **4528 obligaciones** · 2914 con proceso · 204 procesos |
| Grafo | 2139 citas norma→norma |
| Interfaz | `preguntar.py` — pregunta, obligaciones, plazos, procesos, impacto, cambios, bitácora |
| Monitor | cron lunes 06:00 · **ahora APLICA los cambios**, no solo los informa |
| Latencia | ~100 s por respuesta |
| Git | `origin/main` al día |

## Crons

```
*/15  watchdog.sh   relanza experimentos caidos
:00   latido.sh     registra si hay trabajo (cuenta solo python, no loops de bash)
:30   runner.sh     si no hay nada corriendo, toma la siguiente de scripts/cola.txt
0 6 * * 1  monitor_run.sh   scrape -> diff -> informe -> APLICAR -> reproceso -> snapshot
```

`scripts/drenar_cola.sh` consume la cola entera sin esperar al cron. Recuenta en cada vuelta.

## Lo cerrado en esta sesión

```
B4.5 vigencia        21 arts derogados fuera del pool, incl. art 23 del DFL 4 (LGSE)
duplicados           55/55 arts de LEY 20936 eran de la LGSE -> citas falsas cortadas
actualizar_norma     antes solo habia ALTA; el monitor no podia aplicar lo que detectaba
aplicar_cambios      conecta monitor -> corpus, con todas las guardas
descubrimiento USO   discrepancias del Panel -> que normativa falta
ranking por dominio  ordenaba por citas: arriba salia proteccion de datos
```

Normas incorporadas: `DS 44` · `DS 88` · `DS 327` (Reglamento LGSE) · `DS 125` · `LEY 21194` ·
`DECRETO 42` (dominio 0.706, el más alto).

## Bugs propios encontrados y corregidos

```
hardcodeo en el pipeline de obligaciones   lista de titulos a mano -> 429 a 1272 candidatos
glosario 43% basura                        615 -> 350 terminos (frases modificatorias)
DECRETO 125 al 2% de su contenido          4 -> 213 articulos
latido contaba loops de bash como trabajo  7 h muertas sin que nadie mirara
drenador no recontaba la cola
actualizar_norma no re-evaluaba dominio    LEY 20720 (insolvencia) 6 -> 415 arts
```

## Guardas que hay que respetar

`scripts/actualizar_norma.py` **es destructivo** y aborta si:
```
identidad        tipo/numero no coinciden
texto  < 90%     un scrape a medias se ve igual que una derogacion masiva
articulado < 90% el texto puede CRECER y el articulado encoger igual (paso con DFL 4:
                 texto 58x mas grande, articulos 330 -> 278)
```
Frenó 2 de 5 actualizaciones. Eso es lo que se espera de ellas.

## Abierto

```
corte de dominio 0.30    no separa en 0.30-0.42. Candidato 0.36, NO aplicado sin medir.
                         LEY 19882 (personal) puntua 0.411, MAS que LEY 21667 que si
                         es del dominio -> ningun corte lo arregla. docs/frontera-mercados.md
3 truncadas reales       LEY 20936 0.63 · LEY 20999 0.58 · LEY 21667 0.53 (ratio arts/texto)
G4 entity resolution     su criterio del backlog ('excluir derogado') NO sirve: 42 -> 42
B5.4 escalar procesos    mapear los 204 a IVTE/reliquidacion/peajes — necesita al usuario
B6.6 vLLM                ultimo camino de velocidad. Cuello RAM 14 GB, distinto del de VRAM
FASE 3.2/3.3/3.4         BLOQUEADA sin set de preguntas con gold
```

## Trampas ya pagadas (no repetir)

```
el eval se media contra si mismo        279 queries con las 3 plantillas del regex
texto_completo con 'Loading'            NO significa corpus roto. Ver manual-operacion.md
buscador de BCN por numero              DECRETO 44 -> ACUERDO 44 de EDUCACION
ranking de faltantes por citas          arriba salen las transversales citadas de paso
dos python peleando la VRAM             desaloja el modelo, ritmo cae 5x. Una tarea a la vez
pkill -f                                mata el propio shell (exit 144). Usar ps + grep '[p]at'
cron que nadie lee                      7 h de 'CORRIENDO' con la GPU en 0%
```

**Piso de ruido medido**: `cita_ok` 6 %, `cita_limpia` 30 %. Nada por debajo es mejora.


---

# Actualización 2026-08-30 — plan maestro v1 completo (20/20)

## Estado

```
normas             122  (46 fuera de dominio)
articulos         4567  (21 derogados · 73 duplicados, ambos fuera del pool)
obligaciones      4997  (4079 con proceso · 294 procesos)
citas norma-norma 2250  TIPIFICADAS por verbo
```

## Lo que cambió

**El grafo dice qué tipo de relación es**, no solo que existe. El verbo del texto lo declara:
```
remite 1950 · aplica 162 · modifica 89 · deroga 2 · complementa 1
24 de esas apuntan al ARTICULO exacto, no a la norma entera
```
`preguntar.py --impacto` separa las que **rompen** algo de las que solo mencionan:
para el DFL 4 muestra `18 remite · 9 modifica · 1 aplica`.

**Dos bugs del parser que comían contenido:**
```
sufijo -N     "Articulo 92-1" se guardaba como "92" -> 40 articulos perdidos,
              29 en el Reglamento de la LGSE. El que sobrevivia era el 92-3,
              con el texto arrancando en "3.- La Comision podra..."
notas de BCN  la nota de vigencia va EN MEDIO del titulo, a veces ocupando
              4 lineas -> nombres de proceso rotos: 231 -> 4
```

**El DFL 4 (la LGSE) tenía 862 obligaciones sin proceso**: su `texto_completo` está truncado a
10.075 chars y el detector veía 1 encabezado en vez de 26. Ahora, si el JSON bajado tiene más
de 1.5× el texto de la DB, se usa ese **sólo para ubicar los títulos**.

## Andamiaje que corre solo

```
scripts/plan_maestro.txt   todo el trabajo pendiente
scripts/trabajar.sh        lo consume entero, con flock, sincronizando en cada vuelta
cron */10                  revive el worker si muere  (probado matandolo)
cron */15 :00 :30          watchdog · latido · runner
```
Pausa manual: `touch .watchdog_off`.

⚠️ El andamiaje falló tres veces en el mismo punto y las tres están cubiertas:
conteo con duplicados (`10/9`), `cp -n` que no sobrescribía, y `cp -f` fuera del bucle.

## Frentes cerrados sin aplicar (con evidencia)

```
exp #57  corte de dominio 0.36   sacaba biocombustibles y matriz energetica
exp #59  clasificador LLM        5 de 9, pedia 8. Y midio contra MI criterio
                                 de frontera, que es lo que estaba en duda
R8       atribuir articulos      ninguna variante correcta: sin cierre se lleva
                                 los propios de LEY 21472, con cierre pierde
                                 los 56 de LEY 20936
```

**Decisión del usuario sobre la frontera (30-08):** *"si tienen info que puede ser útil déjalas
no más, pero mapea bien la norma y sus relaciones"*. Por eso nada se saca del corpus y el foco
pasó al grafo de relaciones.
