# Handoff — 2026-09-03

Reemplaza a `handoff-2026-08-31.md`. Con leer esto alcanza.

## Estado del corpus

```
normas          124   (48 fuera de dominio -> 76 dentro)
articulos      4999   (15 derogados · 103 duplicados, ambos fuera del pool)
obligaciones   3634   (3556 con proceso · 270 procesos)
citas          2777   norma->norma, TIPIFICADAS por el verbo del texto
```

## LO MÁS IMPORTANTE DE ESTA JORNADA

**El sistema no respondía la mitad de los tipos de pregunta, y las métricas no lo veían.**

Se le hicieron preguntas reales por primera vez. Diagnóstico con 12 preguntas sobre 9 tipos,
taxonomía tomada del estándar (CRAG/MultiHop-RAG + LegalBench + legal RAG):

```
                  think=OFF   think=ON
rule-recall         2/2    ->   2/2     definicion, UNA fuente
null-rechazo        2/2    ->   2/2     fuera del corpus
bridge-multihop     1/1    ->   1/1
temporal-vigencia   1/1    ->   1/1
rule-application    0/1    ->   1/1  <- exige SINTETIZAR
temporal-plazo      0/1    ->   1/1  <-
comparison-multi    0/2    ->   2/2  <-
aggregation         0/1    ->   1/1  <-
interpretation      0/1    ->   1/1  <-
                    6/12        12/12
```

**El corte es nítido:** cerraba lo que sale de UNA fuente, no cerraba lo que hay que
sintetizar. En esos casos devolvía el **razonamiento crudo en inglés**, sin respuesta final,
consumiendo 150-200 s de presupuesto.

`cita_ok` marcaba **87.5 %** con esto pasando. Los sets los escribí yo y son casi todos
`rule-recall` — el único tipo que ya funcionaba. Ver `scripts/exp_tipos_pregunta.py` y
`data/eval/queries_tipos_v1.jsonl`.

## ⚠️ ERROR MÍO, PENDIENTE DE CORREGIR

Encolé el pareado que debía decidir `think=True` y **reusé el script equivocado**:
`exp_selfcons_n1.py` togglea `self_consistency_n` (1 vs 3), **no** `ollama_think`. Se gastaron
~6 h de GPU repitiendo el exp #54.

```
lo que dio (114 pares)   cita_ok 66 -> 66 · cita_limpia 27 -> 35
lo que ES                n=1 vs n=3, NO think. NO sirve para la decision.
```

**Lo que falta:** un pareado real de `think`. Hay que escribir el script (o parametrizar
`exp_selfcons_n1.py` para que toglee `cfg.settings.ollama_think` en vez de
`self_consistency_n`).

**Criterio ya fijado** (`docs/plan-operacion.md`, exp #63):
```
adoptar think=True si  cita_ok cae <= 3  Y  cita_limpia NO cae
```
`cita_limpia` no puede caer: think=True existe para que el modelo deje de rociar citas
mientras delibera.

⚠️ Y hay antecedente en contra: **GEN8 midió que `think=True` pierde 16 golds por rechazar**
en el set grande de `rule-recall`. Las 2 preguntas de ese tipo del diagnóstico no alcanzan
para desmentirlo.

⚠️ Costo confirmado: 150-200 s → **162-377 s** en los casos duros.

## Config relevante

```
ollama_think = False              <- el flag a decidir
ollama_num_predict_think = 6000   <- ya calibrado; con 2000 se queda sin tokens antes
                                     de responder y devuelve vacio en ~55% de los casos
```

## Andamiaje

```
scripts/plan_maestro.txt   todo el trabajo pendiente
scripts/trabajar.sh        lo consume entero · flock · sincroniza en cada vuelta
cron */10                  revive el worker  ·  */15 watchdog · :00 latido · :30 runner
cron lunes 06:00           monitor: scrape -> diff -> informe -> APLICAR -> reproceso
pausa manual               touch .watchdog_off   (verificado: el cron no arranca nada)
```
⚠️ **El límite de GPU NO se toca solo** (se sacó del cron). No sobrevive un reinicio:
`./scripts/gpu_modo.sh <W>`.

## Lo que ya está cerrado con medición

```
#55  answer_doc_limit   125.9 s contra objetivo de 45 s
#56  batching paralelo  25.0 s vs 13.8 s secuencial
#61  vLLM               20 G de modelo contra 7 G de RAM libre -> NO ENTRA
#57  corte dominio 0.36 sacaba biocombustibles (0.300) y matriz energetica (0.301)
#59  clasificador LLM   5 de 9, pedia 8
#60  monitor            corrio 2 h SOLO en produccion: 11 cambios, 4 aplicados,
                        DFL 4 protegido por la guarda de articulado
```

**562 artículos que el parser no veía**, recuperados: sufijo `-N` (40) · forma `Art.` (334) ·
ordinales latinos (73) · grado antes del guion y letra (115).

## Lo que falta

```
1. PAREADO REAL DE think     <- lo mas importante. Script equivocado, hay que rehacerlo
2. preguntas tuyas           20-40, para saber QUE TIPOS pregunta el equipo y con que peso
3. B5.4                      mapear los 270 procesos a los reales de la subgerencia
4. corpus                    plateau: mas PDFs de discrepancias, hay que buscarlos
5. mas RAM                   14 G -> 32 G reabriria vLLM
```
