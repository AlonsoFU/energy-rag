# Handoff — 2026-08-31

Reemplaza a `handoff-2026-08-26.md`. Con leer esto alcanza.

## Estado

| | |
|---|---|
| Corpus | **124 normas** · 4580 artículos · 21 derogados · 73 duplicados (los dos fuera del pool) |
| Foso | **4999 obligaciones** · **4895 con proceso (97 %)** · 331 procesos · 1170 con plazo |
| Grafo | **2426 citas norma→norma, tipificadas** por el verbo del texto |
| Interfaz | `preguntar.py` — pregunta · obligaciones · plazos · procesos · impacto · cambios · bitácora |
| Monitor | cron lunes 06:00, **aplica** los cambios que detecta |
| Latencia | ~100 s |

## Lo grande de esta jornada: el parser no veía 562 artículos

Todos salieron del mismo método —preguntar **por qué una obligación no tiene proceso**—
porque el asignador necesita ubicar el artículo en el texto para saber bajo qué título cae.

```
sufijo -N            40   "Articulo 92-1" se guardaba como "92". El unico 92 que
                          sobrevivia era el 92-3, con el texto arrancando en "3.- La
                          Comision podra...". 29 en el Reglamento de la LGSE.
forma "Art. N"      334   DECRETO 3386 escribe "Art. 17"; se hallaba UN articulo suyo
ordinales latinos    73   "135 quater" CON TILDE y "quinquies" en adelante
grado y letra       115   "72°-1" (el grado va ANTES del guion) y "3º A"
                    ---
                    562 articulos
```

Guarda que valió: la versión laxa de la regla de la letra daba 388 pero incluía basura
(`1\nd`); la estricta da 115 limpios. **Medido antes de aplicar.**

Efecto: `articulo no ubicado` **376 → 35**, cobertura de proceso **61 % → 97 %**.

## El grafo ahora dice qué tipo de relación es

El verbo del texto lo declara, no hay que inferirlo:
```
remite 1950 · aplica 162 · modifica 89 · deroga 2 · complementa 1
24 de esas apuntan al ARTICULO exacto
```
`preguntar.py --impacto 258171` separa lo que **rompe** de lo que solo menciona:
`18 remite · 9 modifica · 1 aplica`, con las 9 leyes que modifican la LGSE marcadas.

## Normas sin subdivisiones

535 obligaciones vivían en normas con **0 encabezados** (LEY 20416 Estatuto PYME, LEY 21305
eficiencia energética, LEY 19657 geotermia). El legislador no dejó agrupamiento interno, pero
el título de la norma nombra su materia. Se usa ese, con prefijo `NORMA` para que se distinga
de un proceso que sí viene del articulado.

## Andamiaje que corre solo

```
scripts/plan_maestro.txt   todo el trabajo pendiente
scripts/trabajar.sh        lo consume entero · flock · sincroniza el plan en cada vuelta
cron */10                  revive el worker  (probado matandolo)
cron */15 :00 :30          watchdog · latido · runner
pausa manual               touch .watchdog_off   (verificado: el cron NO arranca nada)
```
⚠️ El andamiaje falló tres veces y las tres están cubiertas: conteo con duplicados (`10/9`),
`cp -n` que no sobrescribía, y `cp -f` fuera del bucle.
⚠️ **El límite de GPU ya NO se toca solo** (se sacó de `retomar.sh` a pedido del usuario).
No sobrevive un reinicio: se fija a mano con `./scripts/gpu_modo.sh <W>`.

## Frentes cerrados sin aplicar, con evidencia

```
exp #57  corte de dominio 0.36    sacaba biocombustibles (0.300) y matriz energetica (0.301)
exp #59  clasificador LLM          5 de 9, pedia 8. Y midio contra MI criterio de frontera,
                                   que es justo lo que estaba en duda
R8       atribuir articulos        sin cierre se lleva los propios de LEY 21472; con cierre
                                   pierde los 56 de LEY 20936
```

## Lo que queda

```
39 obligaciones   sin estructura ni titulo util
35 articulos      no ubicables
29               en normas que TRANSCRIBEN articulado (a proposito)
vLLM             ultimo camino de velocidad. Cuello RAM 14 GB, distinto del de VRAM
FASE 3.2/3.3/3.4 BLOQUEADA sin preguntas reales con gold
corpus           en plateau: las candidatas que quedan necesitan resolver idNorma por
                 TITULO con busqueda web, la cola no puede sola
```

**Decisión del usuario sobre la frontera:** *"si tienen info que puede ser útil déjalas no más,
pero mapea bien la norma y sus relaciones"*. Nada se saca del corpus; el foco es el grafo.
