# PLAN MAESTRO v28 (2026-09-14) -- exp #77: reatribuir la cita por procedencia
#
# Diagnostico 9/9 del 17 % de held-out en prosa: el modelo copia bien y ETIQUETA mal (toma la
# referencia de notas BCN, "[Art. primero N° 8, d)]", o inventa el numero). Criterio fijado antes
# en docs/plan-operacion.md. Base de comparacion: qonly2_dev / qonly2_holdout (config adoptada).
# La config adoptada ya trae quote_only=True y quote_max=2; solo se prende la reatribucion.
# Corre con embed_4b_num_ctx=4096 (vectores identicos, 3.71 GB de RAM en vez de 9.8 GB).
reatr_dev|env PYTHONPATH=. SOLO_ON=1 FLAGS=answer_quote_reatribuir SET=data/eval/queries_operativas_v1.jsonl NAME=reatr_dev venv/bin/python -m scripts.exp_think_paired
reatr_holdout|env PYTHONPATH=. SOLO_ON=1 FLAGS=answer_quote_reatribuir SET=data/eval/queries_fraseos_v1.jsonl NAME=reatr_holdout venv/bin/python -m scripts.exp_think_paired
