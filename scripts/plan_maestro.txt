# PLAN MAESTRO v11 — re-medir los tipos con la config ADOPTADA (de verdad esta vez)
#
# v10 fallo en silencio: `tipos_v2` resumio el archivo del 01-09, termino en 7 s, imprimio el
# resultado viejo (6/12, think=OFF) y el runner lo marco OK. Ahora cada corrida usa su propio
# NAME y el banner imprime la config EFECTIVA.
tipos_adoptado|env PYTHONPATH=. NAME=tipos_adoptado venv/bin/python -m scripts.exp_tipos_pregunta
