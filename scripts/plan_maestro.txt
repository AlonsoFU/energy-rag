# PLAN MAESTRO v16 — ¿el gold no esta en el pool, o esta y el rerank lo hunde?
#
# donde_falla (v15) dio: de 50 fallos, 84% son de RETRIEVAL -- el gold no llega al top-10.
# Eso parte en dos, y las soluciones no se parecen:
#   esta en el pool de 50 y no en el top-10  -> es el RERANK. Barato de arreglar.
#   no esta ni en los 50                     -> es la GENERACION DE CANDIDATOS (embedding,
#                                               BM25, aliases). Caro, y es otro frente.
# Misma corrida, TOPK=50 (la profundidad del pool antes del rerank). La diferencia entre las
# dos tablas ES la respuesta.
donde_falla50|env PYTHONPATH=. TOPK=50 SET=data/eval/queries_operativas_v1.jsonl RES=data/eval/results/def_dev/result.json venv/bin/python -m scripts.diag_donde_falla
