# PLAN MAESTRO v15 — ¿los fallos son de retrieval o de generacion?
#
# Con la config adoptada el set operativo queda en 64/114 cita_ok y 45/114 cita_limpia, y el
# grueso esta en UN frente: cx_coloquial, 50 de las 114 queries, 11/50 usables. Nunca se midio
# en cual de las dos mitades del sistema esta el problema, y las soluciones no se parecen:
#   gold NO llega al pool   -> RETRIEVAL. Generar mejor no lo arregla.
#   gold llega y no se cita -> GENERACION. Mas recall no lo arregla.
# Solo retrieval, ~2 s por query: cruza el pool con las respuestas YA generadas del pareado.
donde_falla|env PYTHONPATH=. SET=data/eval/queries_operativas_v1.jsonl RES=data/eval/results/def_dev/result.json venv/bin/python -m scripts.diag_donde_falla
